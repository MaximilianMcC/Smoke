class AppManager
{
	public static string GetRunnerPath()
	{
		// TODO: If smoke is not already installed then tell to install
		//? "Smoke cannot be found on this machine. Use `smoke install` to install it."
		return "erhm";
	}
}